[project]
name = "idf-optimal-home2work"
version = "0.1.0"
dependencies = [
    "dbt-bigquery",
    "gcsfs>=2025.12.0",
    "google-cloud-storage",
    "pandas",
    "pyarrow",
]
requires-python = ">=3.11,<3.12"
